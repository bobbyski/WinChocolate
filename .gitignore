.build/
.build-mac/
# Scratch path for the Docker Linux builds — a second .build by another name.
.build-linux-clean/
Run/
demo.stderr.txt
demo.stdout.txt
.freebirdStudio
*.orig
.freebird/
# PackageToJS output for the WebAssembly demos: generated by build-wasm.sh,
# and the .wasm alone is ~76 MB in debug.
Demo/*/WebBuild/
.omegaIDE/

# Scratch path for Tools/run-contract-tests.sh (the WASI contract-suite runner).
.build-wasm-tests/
